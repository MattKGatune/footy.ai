[pytest]
testpaths = tests
addopts = -ra
# Layer markers — unit runs everywhere; integration/smoke (network) added later.
markers =
    integration: hits real R2 / external services (deselect with -m "not integration")
    smoke: runs against the live production deployment
